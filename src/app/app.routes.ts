import { Routes } from '@angular/router';
import { DynamicFormPreviewComponent } from './pages/dynamic-form-preview/dynamic-form-preview.component';

export const routes: Routes = [
    {
        path: '',
        component: DynamicFormPreviewComponent
    }
];
